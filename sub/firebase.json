{
  "packageRules": [
    {
      "matchUpdateTypes": ["major"],
      "matchPackagePatterns": ["^firebase"],
      "labels": ["firebase"],
      "groupName": "firebase packages",
      "prPriority": 1
    }
  ]
}